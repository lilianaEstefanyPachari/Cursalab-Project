export interface VideoData {
  title: string;
  instructor: string;
}

export interface ChapterData {
  id?: string;
  title: string;
  url: string;
  notes: string;
  quiz: string;
  resources: string;
}
